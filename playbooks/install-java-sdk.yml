- name: install Java SDK
  hosts: all
  remote_user: "{{ssh_user}}"
  gather_facts: no
  tasks:
#---------------------------------------------------------------------------------------------
  - name: "ensure 'default-jdk' installed (latest)"
    become: true
    become_method: sudo
    apt:
      name: default-jdk
      state: latest
      update_cache: yes
