- name: install KDE Connect
  hosts: all
  remote_user: "{{ssh_user}}"
  gather_facts: no
  tasks:
#---------------------------------------------------------------------------------------------
  - name: ensure 'kdeconnect' is at the latest version
    become: true
    become_method: sudo
    apt:
      name: kdeconnect
      state: latest
      update_cache: yes
