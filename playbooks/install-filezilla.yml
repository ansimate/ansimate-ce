- name: install FileZilla
  hosts: all
  remote_user: "{{ssh_user}}"
  gather_facts: no
  tasks:
#---------------------------------------------------------------------------------------------
  - name: "ensure 'filezilla' installed (latest)"
    become: true
    become_method: sudo
    apt:
      name: filezilla
      state: latest
      update_cache: yes
