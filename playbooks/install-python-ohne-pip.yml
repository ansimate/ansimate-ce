- name: install Python (ohne PIP)
  hosts: all
  remote_user: "{{ssh_user}}"
  gather_facts: no
  tasks:
#---------------------------------------------------------------------------------------------
  - name: "ensure 'python3' installed (latest)"
    become: true
    become_method: sudo
    apt:
      name: python3
      state: latest
      update_cache: yes
